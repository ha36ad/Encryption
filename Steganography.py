import cv2
import numpy as np

#convert the image to binary in the form of a string
def binconv (i):


#encode the message within the image
    def encode(image_name, raw_data):
        #reads the image
        read_image = cv2.imread(image_name)
        
